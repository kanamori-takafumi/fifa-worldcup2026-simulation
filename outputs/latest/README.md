# Latest curated output snapshot

This directory contains the public, compact snapshot of the latest two-stage run.

Included:

- summary and metadata
- group-stage probabilities and selected scenario tables
- conditional knockout probabilities for scenario `C01`
- coherent projected bracket CSVs
- recent Elo preprocessing CSV/JSON audit tables

Omitted intentionally:

- non-compact `visuals/`
- duplicate compact HTML/SVG, because the public copy is under `docs/`
- large row-level dynamic Elo audit sample CSVs
- progress JSON and local OS metadata
